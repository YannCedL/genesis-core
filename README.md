# genesis-core

shared models for the osint project

## install

```bash
pip install -e .
```

## what it does

- defines evidence types (fact, observation, inference etc)
- provenance tracking
- result contract that all engines use


